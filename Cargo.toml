[package]
name = "strixctrl"
version = "0.1.0"
edition = "2024"
default-run = "strixctrl"

[dependencies]
eframe = "0.29"
egui = "0.29"
egui_plot = "0.29"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zbus = { version = "4", features = ["tokio"], optional = true }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "sync", "time"], optional = true }

[features]
daemon = ["zbus", "tokio"]

[[bin]]
name = "strixctrld"
path = "src/bin/strixctrld.rs"
required-features = ["daemon"]
